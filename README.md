# GPSD Time Set

Set your system time on Linux or Windows via GPSD with a waiting period supported for leap second sync waiting.
Requires to by run as root/administrator to perform changes to system time.

GPSD uses a UTC based output so use `OFFSET_DURATION` env variable to set the time offset.

(C) Captain ALM 2026 - BSD 3-Clause License